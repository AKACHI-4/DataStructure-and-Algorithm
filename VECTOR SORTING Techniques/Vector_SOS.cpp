#include<bits/stdc++.h>
#include<vector>

using namespace std;

int main()
{
    // vector<int> v; 
    // v.push_back(1);    
    // v.push_back(2);    
    // v.push_back(3);    
    // v.push_back(4);    
    // v.push_back(5);

    // for(int i=0;i<v.size();i++)
    // {
    //     cout<<v[i]<<endl; 
    // }

    // /* To get an iterator(*same as indexes in array) in vector */
    // vector<int>:: iterator it; 
    // for(it=v.begin();it!=v.end();it++)
    // {
    //     cout<<*it<<endl;
    // } // 1 2 3 

    // for(auto element : v)
    // {
    //     cout<<element<<" ";
    // } // 1 2 3

    // v.pop_back(); // 1 2 (` *3 will be popped out from vector )

    // vector<int> v2 (3,100);
    // // 100 100 100

    // swap(v,v2);
    // for(auto element : v)
    // {
    //     cout<<element<<" ";
    // } // 100 100 100 
    // for(auto element : v2)
    // {
    //     cout<<element<<" ";
    // } // 1 2 

    
    // sort(v.begin(), v.end());

    return 0;
}