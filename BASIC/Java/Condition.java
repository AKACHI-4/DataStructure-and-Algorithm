import java.util.Scanner;

public class Condition {
    public static void main(String[] args){
        Scanner input = new Scanner(System.in);

        // int a = input.nextInt(); 
        // int b = input.nextInt(); 
        // int c = input.nextInt(); 

        // int max = a;
        
        // if(b>max)  
        //     max = b; 
        // if(c>max)
        //     max = c; 
            
        // int max = Integer.MIN_VALUE; 

        // if(a>b && a>c)
        //     max = a; 
        // else if(b>a && b>c)
        //     max = b; 
        // else
        //     max = c;

        // Integer max = Math.max( a, Math.max(b,c) );

        // System.out.println(max);

        // char ch = input.next().trim().charAt(0);

        // if(ch>=65 && ch<=90)
        //     System.out.println("Uppercase");
        // else
        //     System.out.println("Lowercase");

        // String str = input.next().trim(); 
        // System.out.println(str);
        // System.out.println(str.charAt(5));

        // int a = input.nextInt();
        // int b = input.nextInt(); 
        // if(a>0 || b<10)
        //     System.out.println(a + " and " + b + " both are in the true good range!!");

        // System.out.print("Enter the position of number you want : ");
        // int n = input.nextInt(); 
        // int fst = 0; 
        // int nxt = 1; 
        // while(n!=2){
        //     int temp = nxt; 
        //     nxt = fst + nxt; 
        //     fst = temp; 
        //     n--; 
        // }
        // System.out.println(n + "th Fibonacci number is : " + nxt);

        int num = 78561111; 

        // System.out.print("Enter the no. which occurence you want in input number : ");
        // int n = input.nextInt(); 
        // int ocur = 0; 
        // while(num!=0){
        //     int rem = num%10; 
        //     if(rem == n)
        //         ocur++; 
        //     num = num/10; 
        // }
        // System.out.println("occurence of no. " + n + " is : " + ocur);

        // System.out.print("Reverse of given input number : ");
        // int val = 0; 
        // while(num!=0){
        //     int rem = num%10; 
        //     val = val*10 + rem;
        //     num = num/10; 
        // }
        // System.out.print(val);
    }
}
